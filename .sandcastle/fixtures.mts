/**
 * Shared test fixtures for tracer-bullet contract.
 *
 * Single source of truth for the minimal body that satisfies all 7
 * concepts plus the Wayfinder map signal. Keeps acceptance/dispatch/
 * factory tests consistent and avoids three-way string duplication.
 */

export const TRACER_BODY =
  `Scope bounded observable outcome\n` +
  `no unresolved design decided\n` +
  `acceptance criteria done when\n` +
  `verification path verify\n` +
  `dependencies blocked by none\n` +
  `small enough for one session\n` +
  `vertical tracer bullet slice end-to-end\n` +
  `Execution is carried into this map`;
