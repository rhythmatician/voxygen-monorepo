# Plan: Factory doc-truth and tracker reconciliation (a6ccf9a baseline)

## Goal
Bring the factory from "promising loop with lying docs and lagging tracker" to a state where a fresh agent can trust `AGENTS.md` + `CONTEXT.md` + authoritative docs + `Factory / Merge Oracle` without needing tribal knowledge. Concretely: reconcile already-landed control-plane work (#46/#47), make highly-retrievable docs match `a6ccf9a` truth (#41), queue the human classification → mechanical purge (#42→#43), remove executable stale instructions from the issue tracker (#9 audit), and fill the two remaining Matt-derived factory gaps (AFK tracer-bullet ticket contract + issue-contract-aware review).

## Success Criteria
- `README.md` describes only `.github/workflows/factory-ci.yml` and `.ci/checks.json` authoritative check; no mention of `java-ci.yml`/`python-ci.yml` or "retained nested workflows" remains. (Owned by #47 — see Key Decisions.)
- `GLOSSARY.md` no longer labels `RealVoxyVolumeWriter` or `VoxelPredictionDecoder` as `[Planned]` — both are `[Current]` with correct source refs (`java/src/main/java/com/rhythmatician/lodiffusion/voxy/RealVoxyVolumeWriter.java`, `VoxelPredictionDecoder.java`). Its `Serialization Format and Morton` section directly states little-endian YZX-linear (SaveLoadSystem3), with no residual "Morton ordered / must apply lin2z" claim.
- `python/docs/NOISE-DESIGN.md` is unmistakably marked **historical** (March 2026 architecture generation: no learned L0, four transitions L4→L1) with date/scope/successor links to `CONTEXT.md` + current per-Level contracts (PR #36, L0–L4) — or is deleted if it contains no unique rationale worth retaining. No unqualified statement presents the historical four-transition/no-L0 architecture as current. (Per #41 contract.)
- Graph is used as retrieval-pollution diagnostic, not authority oracle: after the doc fixes, obsolete live files are deleted or retained historical files visibly advertise their status; a follow-up `graphify` run is then used to confirm reduced retrievable legacy communities.
- GitHub issues #46 and #47 either closed with recorded disposition or updated to reflect reality (PR #45 merged, nested workflows already absent), with no dangling "active implementation" language.
- Open agent-targetable issues (starting with #9) no longer contain executable stale strings (`./dev/verify-java`, `spec/`, `fixtures/golden/`, `verify-all`, `java-ci.yml`, `python-ci.yml`). Audit is recorded.
- Two new factory issues exist: (1) generic AFK implementation-ticket contract / tracer-bullet decomposition rule, (2) reviewer must review against original issue contract + acceptance criteria in a fresh context.
- No change to `.github/workflows/factory-ci.yml`, `.ci/checks.json`, `.ci/voxy-artifact.json`, `.sandcastle/`, or `python/docs/VOXY-FORMAT.md` / `python/voxel_tree/voxy_format` unless explicitly required and reviewed as C5 control-plane.

## Context And Current Facts
**Baseline verified at `a6ccf9a` (local HEAD matches remote `main`):**
- `git log --oneline -5` shows `a6ccf9a` Merge PR #55, `2caaf5a`, `01cb9df` Merge PR #36 (L0-L4 contracts), `d9aaa7f` Merge PR #54, `e88d3c6` Merge PR #53. Pasted `bb8bb48` is behind.
- `AGENT_RULES.md` is gone from tree ( `ls AGENT*` returns only `AGENTS.md`). `AGENTS.md` is intentionally tiny (10 lines, router to `docs/agents/issue-tracker.md`, `triage-labels.md`, `domain.md`) — per skill instruction this is the desired small pushed context.
- CI is now single-root: `.github/workflows/factory-ci.yml` only ( `ls .github/workflows` = `factory-ci.yml`). `.ci/checks.json` `authoritativeCheck: "Factory / Merge Oracle"` with lanes `repository/factory/python/java/cross-language/performance/security` and requirements covering `X-01..X-06` (cross-language) and `X-07..X-09` (java). `controlPlanePrefixes` + `controlPlaneFiles: ["AGENTS.md"]` + `.ci/README.md` describes `factory-control-plane` environment gate and pinned Voxy artifact — matches PR #45 intent quoted in prompt.
- `java/src/main/java/com/rhythmatician/lodiffusion/voxy/RealVoxyVolumeWriter.java` and `VoxelPredictionDecoder.java` both exist and are referenced from production code (`LodGenerationService.java`, `HeightmapFallbackGenerator.java`). `CONTEXT.md` already lists `VoxelPredictionDecoder` and `VoxelVolumeWriter` as current canons without `[Planned]` — so `CONTEXT.md` is ahead of `GLOSSARY.md`.
- `GLOSSARY.md` still says `### RealVoxyVolumeWriter [Planned]` (line ~276) and `### VoxelPredictionDecoder [Planned]` (line ~284), contradicting source existence. Also still asserts Morton-order serialization `Serialization Format and Morton (Z-curve) Order` (line ~221) claiming `indices in Morton order` + `Python readers must apply lin2z` — but `#44`/`python/docs/VOXY-FORMAT.md` + `python/voxel_tree/voxy_format` now teach little-endian, YZX-linear, with tested semantics. GLOSSARY's serialization claim is directly wrong and must be replaced (not left with errata).
- `README.md:40-46` still claims authoritative workflows are `.github/workflows/java-ci.yml` and `.github/workflows/python-ci.yml` and that `java/.github/workflows/ci.yml` / `python/.github/workflows/ci.yml` are "retained for history" — both false at `a6ccf9a`. Per ChatGPT correction, this is #47 scope ("repository references no longer imply nested workflows execute"), not #41.
- `python/docs/NOISE-DESIGN.md` is **not** current despite correctly describing router6 as dropped: it still states "No LOD0 generation" and "All 4 LOD transitions" (Init→L4, L4→L3, L3→L2, L2→L1), which is incompatible with the merged five live L0–L4 contracts from PR #36. Per #41 it must be removed or unmistakably marked historical (March 2026 generation) with successor links to `CONTEXT.md` + current contracts. Router6 correctness does not make the architecture current.
- Graph reports at `graphify-out/` still surface legacy communities — expected because they index docs that haven't been purged yet (that's #41-#43's job).
- Tracker state could not be verified locally: `gh` CLI not found in this shell (`gh issue list` → `command not found`). Issues #9, #41, #42, #43, #46, #47, #48 described in prompt; bodies/labels/comments not locally fetchable. Must be verified via `gh issue view` in an environment with `gh` auth.
- Two Matt-derived gaps still absent as issues: no AFK/tracer-bullet ticket contract; reviewer instructions still "understand intent from diff and commits" without original issue contract (prompt's quote). This matches `docs/agents/` not containing a reviewer-contract doc.

## Constraints And Non-goals
- Do not weaken tests/oracles to get green; do not generate a substitute Voxy API or synthetic ONNX model; infrastructure failure is never a pass (`.ci/README.md`, `.ci/voxy-artifact.json` pinned).
- `external/` is read-only; Voxy facts flow `external fact → tested executable reference (voxy_format) → documentation`, not via markdown assertions.
- Do not autonomously merge or self-approve control-plane changes (C5). `factory-control-plane` requires human `Review deployments → Approve and deploy`. #48 stays human-only (GitHub ruleset requiring stable oracle check).
- Do not reintroduce `spec/`, `fixtures/golden/`, `verify-all`, nested workflows, or `AGENT_RULES.md`.
- Non-goals: product work #22/#25/#27/#28, performance/security lanes, new model training, or rewriting `VOXY-FORMAT.md`. Keep scope to doc truth + tracker hygiene + missing factory contracts.

## Key Decisions
1. **Verify before editing; treat graph as detector, not oracle.** Decision: fix only docs proven lying at `a6ccf9a` (GLOSSARY's two `[Planned]` + serialization, `NOISE-DESIGN.md` historical status, plus any other doc that contradicts `VOXY-FORMAT.md`/`CONTEXT.md`/source existence). Graph communities are symptoms; #42 human classification decides the broad purge list, #43 executes it. Success is that obsolete live files are deleted or historical files visibly advertise status, with graph as follow-up diagnostic — not "graph no longer attributes authority." Alternative (purge everything the graph flags) rejected.
2. **Reconcile #46/#47 before #41.** Decision: update #46/#47 first because they describe the CI architecture that #41's doc fixes depend on. Move README CI correction from #41 to #47 — #41 scope is `GLOSSARY.md`, `NOISE-DESIGN.md`, and broken refs; #47 explicitly requires "repository references no longer imply nested workflows execute" and the stale README is precisely that. Alternative (start with docs or keep README in #41) rejected — would violate issue contracts and blur ownership.
3. **GLOSSARY fix is a direct replacement, not errata.** Decision: change `[Planned]` → `[Current]` with correct source refs, and *replace* the Morton-order sentence with the tested truth: little-endian YZX-linear (SaveLoadSystem3), high-nibble = block light, low-nibble = sky light, fixed-width signed section coords, with pointer to `python/docs/VOXY-FORMAT.md` + `python/voxel_tree/voxy_format`. Leaving the false claim with a "see this other file" note preserves semantic poisoning. Alternative (errata note) rejected per ChatGPT correction.
4. **Issue hygiene updates bodies, not historical comments.** Decision: scan all open agent-targetable issues for retired executable strings (`./dev/verify-java`, `spec/`, `fixtures/golden/`, `verify-all`, `java-ci.yml`, `python-ci.yml`) and fix *bodies* (current work contracts) via `gh issue edit`. For stale comments that could mislead, add a newer superseding comment rather than rewriting history. #9 is a body-level fix (open Wayfinder prototype). Alternative (mechanically rewrite comments) rejected.
5. **New factory issues are end-to-end tracer bullets, `ready-for-agent`.** Decision: create two issues that each deliver working behavior in one vertical slice: (a) AFK tracer-bullet ticket contract — define generic `ready-for-agent` decomposition rule, enforce it in `docs/agents/` / `.sandcastle/` templates, add test that `ready-for-agent` tickets are one slice, and prove with a sample. (b) Reviewer against original contract — replace the live instruction "Understand intent from diff and commits" with a prompt that passes original issue body + acceptance criteria into a fresh reviewer context alongside diff/commits/standards, plus plumbing/tests proving a review can compare implementation vs acceptance. No design-only then second implementation issue — that reproduces the horizontal decomposition we're eliminating. Labels: `ready-for-agent` (design is already decided; `controlPlane` will still gate integration), add `agent:implement` only when dispatch is desired. Alternative (`ready-for-human` contract-only + later implementation) rejected per Matt-derived control-plane semantics.
6. **NOISE-DESIGN handling per #41 contract.** Decision: do not confirm as current. Mark unmistakably historical (header: date March 2026, scope: four-transitions/no-L0 generation, successor: `CONTEXT.md` + current per-Level contracts L0–L4 from PR #36 + dense baseline + minimum-representation program) or delete if no unique rationale survives. Alternative (leave as "current, router6 is correct") rejected — directly contradicts #41 acceptance.

## Recommended Approach
Work in strict sequence so each step's evidence is available for the next (ChatGPT-corrected flow: `#46 reconcile → #47 finish/README → #41 GLOSSARY + historicalize/delete NOISE-DESIGN → #42 human classification → #43 purge → tracker stale-instruction audit → create and dispatch the two end-to-end factory issues`):

1. **Ground truth** — `git fetch origin && git log --oneline origin/main -10` and `gh issue view` for #9, #41, #46, #47, #48 to confirm current bodies/labels/comments. Record deltas vs. prompt's description.
2. **Tracker reconciliation — #46** — narrowly update #46 ("PR #45 merged 2026-08-15 709aebe, acceptance = passing/failing evidence + final docs") — do not close solely because PR #45 merged; remaining acceptance (intentional failure behavior, final documentation reconciliation) must still be verified. This is non-C5 (issue edits).
3. **Tracker reconciliation — #47 + README** — update #47 ("nested workflows absent since 709aebe, unique behavior disposition recorded") and, as part of #47, patch `README.md` CI section to name only `.github/workflows/factory-ci.yml` + `.ci/checks.json` + `Factory / Merge Oracle`, removing the `java-ci.yml`/`python-ci.yml` and "retained for history" claims. #41 no longer owns README.
4. **Doc-truth slice (#41)** — Promote `GLOSSARY.md`'s two entries to `[Current]` and *replace* `Serialization Format and Morton`'s false Morton-order claim with the tested truth (little-endian YZX-linear, block-high/sky-low nibbles, fixed-width signed coords, pointer to `VOXY-FORMAT.md` + `voxy_format`). Historicalize or delete `python/docs/NOISE-DESIGN.md` per #41 contract (header: Historical — March 2026 architecture generation, no learned L0, four transitions; successors: `CONTEXT.md` + current per-Level contracts L0–L4 (PR #36) + dense baseline). Fix `GLOSSARY.md` broken link to removed `OCTREE-GENERATION-DESIGN.md`. No other file moved/removed.
5. **Queued human gate (#42→#43)** — publish/refresh the classification issue (#42) listing the remaining obsolete corpus candidates (graph legacy communities, `MASTER_PLAN.md` sections, etc.) for human triage; #43 remains blocked until #42 is closed. This plan does not perform the purge.
6. **Stale-instruction audit** — `gh issue list --state open --json number,title,body,labels,comments` search for retired strings; update *bodies* (current contracts) via `gh issue edit` (starting with #9: `./dev/verify-java` + `spec/`/`fixtures/golden/`). For stale *comments* that could mislead, add a superseding comment rather than rewriting history. Record audit.
7. **Missing contracts — two end-to-end tracer bullets (`ready-for-agent`)** — create two issues that each deliver working behavior in one slice: AFK tracer-bullet ticket contract and reviewer-against-original-contract (replacing "Understand intent from diff and commits" end-to-end with plumbing/tests). Control-plane gating is via `factory-control-plane` at merge, not via `ready-for-human` implementation.

Validation is lane-local: `npm run typecheck && npm test` (factory lane), `pytest`/`pyright` if python docs touched, `factory-ci.yml` dry-run via `npm run ci:oracle -- --command plan`.

## Work Plan
### Phase 0 — Baseline (no diff)
- **0.1 Fetch and diff baseline.** `git fetch origin && git status && git log --oneline -10` and `ls .github/workflows && cat .ci/checks.json && cat AGENTS.md`. Confirm `a6ccf9a` is current and `gh` auth: `gh auth status`.
  - Depends: none. Owner: agent.

### Phase 1 — Tracker reconciliation (issue edits only, no code)
- **1.1 Reconcile #46.** `gh issue view 46 --comments` then `gh issue edit 46 --body "<reconciled>"` + `gh issue comment 46 --body "Reconciled at a6ccf9a: PR #45 merged at <sha>..."`. Close only if passing/failing evidence + final docs acceptance is met; otherwise relabel/comment.
  - Depends: 0.1. Owner: agent.
- **1.2 Reconcile #47.** `gh issue view 47 --comments`, verify `ls .github/workflows` has no nested files, then edit body to record disposition of unique nested-workflow behavior (or "none") and close if appropriate.
  - Depends: 0.1. Owner: agent.
  - Note: #48 is not touched beyond confirming it stays `ready-for-human` (human platform config).

### Phase 2 — Doc truth (#41, minimal patch)
- **2.1 Fix README CI section.** Replace `README.md:40-46` with single-source truth: `.github/workflows/factory-ci.yml` as the only discovered workflow, `.ci/checks.json` as registry, `Factory / Merge Oracle` as authoritative check. Remove `java-ci.yml`/`python-ci.yml` and "retained for history" lines. Keep root-delegation Gradle/Python sections unchanged.
  - Depends: 1.1, 1.2. Owner: agent.
- **2.2 Promote GLOSSARY entries.** Edit `GLOSSARY.md:276-284` headings `[Planned]` → `[Current]` for `RealVoxyVolumeWriter` and `VoxelPredictionDecoder`, add source refs matching `CONTEXT.md` style, and add 2-line errata under `Serialization Format and Morton` noting `python/docs/VOXY-FORMAT.md` + `python/voxel_tree/voxy_format` is the tested little-endian YZX-linear truth (no Morton on storage path). No other GLOSSARY restructuring.
  - Depends: 2.1. Owner: agent.
- **2.3 Optional quick wins if proven lying.** Only if `grep` proves contradiction: fix any other doc line that still asserts the historical "four transitions, no learned L0" as current. Otherwise list candidates for #42 without editing.
  - Depends: 2.2. Owner: agent.

### Phase 3 — Human classification queue
- **3.1 Refresh #42 bodies.** Ensure #42 lists the remaining obsolete corpus (graph legacy communities, `python/docs/MASTER_PLAN.md` / `MODEL-CONTRACT.md` stale sections) as a checklist for human triage. No file deletions here.
- **3.2 Gate #43.** Confirm #43 is blocked by #42 (native dependency or `Blocked by: #42` line). No execution.

### Phase 4 — Stale executable instructions
- **4.1 Audit open issues.** `gh issue list --state open --json number,title,body,labels,comments --jq` then search for `verify-java|spec/|fixtures/golden|verify-all|java-ci\.yml|python-ci\.yml`. Starting with #9: `gh issue view 9 --comments`.
- **4.2 Patch #9 and any other hits.** `gh issue edit <n> --body` to replace stale gate with current (`npm run typecheck && npm test`, `uv sync`/`pytest`, `Factory / Merge Oracle`), plus `gh issue comment <n>` audit note. Do not close unless work is done.
  - Depends: 0.1. Can run in parallel with Phase 2.

### Phase 5 — Missing factory contracts (new issues)
- **5.1 AFK tracer-bullet ticket contract.** `gh issue create --title "Factory: AFK implementation-ticket contract (tracer-bullet)" --body` describing: each `ready-for-agent` ticket must be one vertical slice (contract → test → implementation → evidence), decomposed so an AFK agent can complete without inventing scope; acceptance = ticket body states slice, Done criteria are checkable via `Factory / Merge Oracle` lanes.
- **5.2 Issue-contract-aware review.** `gh issue create --title "Factory: reviewer reviews against original issue contract" --body` describing: reviewer prompt must include original issue body + acceptance criteria + diff + commits + coding standards in a fresh context; alternative "intent from diff/commits" is rejected; acceptance = reviewer instructions reference this issue and a sample review cites the original contract.
  - Depends: 4.2 (so new issues don't inherit stale template). Owner: agent.

## Validation Plan
- **Phase 0:** `git fetch origin && git status --short --branch` shows `main...origin/main` clean (except `.agents/plans/`); `ls .github/workflows` = `factory-ci.yml`; `cat .ci/checks.json | jq .authoritativeCheck` == `"Factory / Merge Oracle"`; `ls AGENT*` has no `AGENT_RULES.md`.
- **Phase 1:** `gh issue view 46/47 --comments` shows reconciled bodies; `gh issue list --state open` no longer lists #46/#47 as "active implementation" if closed, or shows updated acceptance. Manual: open PR #45 and confirm merged SHA.
- **Phase 2:** `grep -n "java-ci\.yml\|python-ci\.yml\|retained for history" README.md` returns 0; `grep -n "\[Planned\].*RealVoxy\|VoxelPrediction" GLOSSARY.md` returns 0 and `grep -n "RealVoxyVolumeWriter.*\[Current\]" GLOSSARY.md` hits; `npm run typecheck && npm test` passes (factory lane); `python -m pyright --version` / `uv run ruff check` if python docs touched (non-blocking).
- **Phase 4:** `gh issue list --state open --json body --jq '.[].body' | grep -c "verify-java\|spec/\|fixtures/golden\|verify-all"` == 0. `gh issue view 9 --json body --jq .body` contains current gates, not stale ones.
- **Phase 5:** `gh issue list --state open --search "Factory: AFK\|Factory: reviewer" --json number,title,labels,body` shows two new issues with acceptance criteria; triage labels applied.
- **E2E (no publish):** `npm run ci:oracle -- --command plan --files changed-files.txt` dry-run on the Phase 2 diff shows correct change classes (not C5 unless `README.md`/`GLOSSARY.md` are classified as non-control-plane — verify against `.ci/checks.json` controlPlanePrefixes). Full `factory-ci.yml` would be validated in a draft PR, not by pushing `main`.
- Highest-risk validation: **Phase 4 audit completeness** — stale strings may live in issue comments, not just bodies. Must fetch `comments` and search both; otherwise #9-style poisoning survives via comments.

## Risks / Rollback
- **Risk: editing #46/#47 bodies loses history.** Mitigation: use `gh issue comment` to record original body excerpt before edit; rollback via `gh issue edit --body` restoring previous content from comment history.
- **Risk: GLOSSARY errata creates new contradiction.** Mitigation: keep edit minimal (status tags + 2-line pointer), don't rewrite serialization section; full reconciliation deferred to #42 classification.
- **Risk: `gh` not authenticated in this shell.** Mitigation: Phase 0 checks `gh auth status`; if unauthenticated, publish plan but defer all `gh issue` writes until auth is provided — doc patches (Phase 2) can still land as a draft PR.
- **Risk: control-plane misclassification.** Mitigation: verify `README.md`/`GLOSSARY.md` are not in `controlPlanePrefixes`/`controlPlaneFiles` before treating as non-C5; if they are, require human approval via `factory-control-plane` environment and do not enable autonomous merge.
- **Rollback:** all file edits are single-commit-per-phase; revert with `git revert <sha>` or `git checkout origin/main -- README.md GLOSSARY.md`. Issue edits are reversible via `gh issue edit` with prior body from comments.

## Open Questions
- What are the current bodies/labels/comments of issues #9, #41, #42, #43, #46, #47, #48 at `a6ccf9a`? (`gh` not available in this shell to fetch — must verify before Phase 1.)
- Does `RealVoxyVolumeWriter`/`VoxelPredictionDecoder` promotion in `GLOSSARY.md` also require updating `graphify-out/GRAPH_REPORT.md` inputs, or is that generated artifact excluded from #41? (Assume generated — not hand-edited.)
- Is `NOISE-DESIGN.md` at `python/docs/NOISE-DESIGN.md` considered "highly retrievable" for #41, or is the intended target a different path that still asserts the old architecture? At `a6ccf9a` it is already correct (router6 dropped), so confirm no other `NOISE-DESIGN.md` exists at `docs/` root (verified absent).
- Should the two new factory issues be labeled `ready-for-human` (human design) or `ready-for-agent` (agent creation is creation-only, implementation of the contracts is separate)? Propose `ready-for-human` for the contract definitions, then `ready-for-agent` for the follow-up implementation of the reviewer prompt change.
