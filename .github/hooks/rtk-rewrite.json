{
  "version": 1,
  "hooks": {
    "PreToolUse": [
      {
        "type": "command",
        "command": "sh -c 'command -v rtk >/dev/null 2>&1 && rtk hook copilot || exit 0'",
        "cwd": ".",
        "timeout": 5
      }
    ],
    "preToolUse": [
      {
        "type": "command",
        "bash": "command -v rtk >/dev/null 2>&1 && rtk hook copilot || exit 0",
        "powershell": "if (Get-Command rtk -ErrorAction SilentlyContinue) { rtk hook copilot } else { exit 0 }",
        "cwd": ".",
        "timeoutSec": 5
      }
    ]
  }
}
