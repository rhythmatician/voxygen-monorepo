"""VoxelTree training scripts — organized by model track.

Each subdirectory contains all code related to a specific model:
- octree/: octree models (init, refine, leaf), dataset, training, export, deployment
- sparse_root/: sparse root models, training, distillation, calibration
- stage1/: density and terrain shaper models, training, extraction
- data_acq/: data acquisition (world generation, voxel import, noise dumps, heightmap extraction)
"""
