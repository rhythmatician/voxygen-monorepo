repos:
  - repo: local
    hooks:
      - id: qgate
        name: Quality Gate
        language: system
        entry: uv run qgate --fix --type-checker dmypy
        types: [python]
        pass_filenames: true
        require_serial: true
