repos:
  - repo: local
    hooks:
      - id: qgate
        name: Quality Gate
        language: system
        entry: uv run --project python qgate --fix
        types: [python]
        pass_filenames: true
        require_serial: true
